import { mat3 } from './mat3';
export declare class vec3 extends Float32Array {
    static readonly zero: vec3;
    static readonly one: vec3;
    static readonly grey: vec3;
    static readonly right: vec3;
    static readonly left: vec3;
    static readonly up: vec3;
    static readonly down: vec3;
    static readonly forward: vec3;
    static readonly backward: vec3;
    static readonly axes: vec3[];
    static readonly infinity: vec3;
    constructor(values?: number[]);
    get x(): number;
    set x(x: number);
    get y(): number;
    set y(y: number);
    get z(): number;
    set z(z: number);
    get xyz(): number[];
    set xyz(xyz: number[]);
    get rgb(): number[];
    set rgb(rgb: number[]);
    get length(): number;
    get squaredLength(): number;
    reset(): vec3;
    copy(dest?: null | vec3): vec3;
    negate(dest?: null | vec3): vec3;
    equals(vector: vec3, threshold?: number): boolean;
    add(vector: vec3, dest?: null | vec3): vec3;
    subtract(vector: vec3, dest?: null | vec3): vec3;
    multiply(vector: vec3, dest?: null | vec3): vec3;
    divide(vector: vec3, dest?: null | vec3): vec3;
    scale(scalar: number, dest?: null | vec3): vec3;
    normalize(dest?: null | vec3): vec3;
    reflect(normal: vec3, dest?: null | vec3): vec3;
    transform(matrix: mat3, dest?: null | vec3): vec3;
    interpolate(v2: vec3, time: number, dest?: null | vec3): vec3;
    serialize(): number[];
    static deserialize(values: number[]): Promise<vec3>;
    static interpolate(v1: vec3, v2: vec3, time: number, dest?: null | vec3): vec3;
    static absolute(vector: vec3, dest?: null | vec3): vec3;
    static minimum(v1: vec3, v2: vec3, dest?: null | vec3): vec3;
    static maximum(v1: vec3, v2: vec3, dest?: null | vec3): vec3;
    static cross(v1: vec3, v2: vec3, dest?: null | vec3): vec3;
    static dot(v1: vec3, v2: vec3): number;
    static distance(v1: vec3, v2: vec3): number;
    static squaredDistance(v1: vec3, v2: vec3): number;
    static direction(v1: vec3, v2: vec3, dest?: null | vec3): vec3;
    static mix(v1: vec3, v2: vec3, time: number, dest?: null | vec3): vec3;
    static add(v1: vec3, v2: vec3, dest?: null | vec3): vec3;
    static subtract(v1: vec3, v2: vec3, dest?: null | vec3): vec3;
    static multiply(v1: vec3, v2: vec3, dest?: null | vec3): vec3;
    static divide(v1: vec3, v2: vec3, dest?: null | vec3): vec3;
    static scale(vector: vec3, scalar: number, dest?: null | vec3): vec3;
    static normalize(vector: vec3, dest?: null | vec3): vec3;
    static sum(...vectors: vec3[]): vec3;
    static difference(...vectors: vec3[]): vec3;
    static product(...vectors: vec3[]): vec3;
    static division(...vectors: vec3[]): vec3;
}
