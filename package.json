{
  "name": "luz",
  "private": true,
  "version": "1.0.0",
  "author": "Matthias Ferch",
  "scripts": {
    "build:utilities": "cd modules/utilities && npm run build",
    "build:vectors": "cd modules/vectors && npm run build",
    "build:core": "cd modules/core && npm run build",
    "build:collisions": "cd modules/collisions && npm run build",
    "build:graphics": "cd modules/graphics && npm run build",
    "build:all": "npm-run-all -s build:utilities build:vectors build:core build:collisions build:graphics"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "webpack": "^5.58.2",
    "webpack-cli": "^4.9.1"
  }
}
